import Toybox.Lang;

//! The smallest barrel the plugin's tests can build and run tests against.
module FixtureBarrel {
    function twice(value as Number) as Number {
        return value * 2;
    }
}
